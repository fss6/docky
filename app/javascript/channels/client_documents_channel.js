import clientDocumentsController from "controllers/client_documents_channel_controller"

const subscriptions = new Map()

export default {
  subscribe(consumer) {
    // Subscriptions are created per-element via Stimulus; this file is a placeholder for importmap.
  }
}
