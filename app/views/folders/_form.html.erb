<%= form_with(model: folder, class: "space-y-6") do |form| %>
  <% if folder.errors.any? %>
    <div class="rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-900 shadow-[var(--shadow-card)]" role="alert">
      <% err_n = folder.errors.count %>
      <p class="font-medium"><%= err_n %> <%= err_n == 1 ? "erro impediu" : "erros impediram" %> salvar esta pasta:</p>
      <ul class="mt-2 list-inside list-disc space-y-0.5 text-red-800">
        <% folder.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="space-y-5">
    <div>
      <%= form.label :account_id, "Conta", class: "app-label" %>
      <%= form.collection_select :account_id, Account.order(:name), :id, :name,
            { prompt: "Selecione uma conta" },
            class: "app-input" %>
    </div>

    <div>
      <%= form.label :name, "Nome", class: "app-label" %>
      <%= form.text_field :name, class: "app-input", autocomplete: "off", placeholder: "Nome da pasta" %>
    </div>
  </div>

  <div class="flex flex-col-reverse gap-3 border-t border-zinc-100 pt-6 sm:flex-row sm:justify-end">
    <%= link_to "Cancelar", folders_path, class: "app-btn-secondary text-center sm:text-left" %>
    <%= form.submit folder.persisted? ? "Atualizar pasta" : "Criar pasta", class: "app-btn-primary cursor-pointer border-0" %>
  </div>
<% end %>
