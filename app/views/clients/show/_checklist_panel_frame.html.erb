<%= turbo_frame_tag "client_checklist_panel" do %>
  <%= render "clients/show/checklist_panel",
        client: client,
        checklist_items: checklist_items,
        period: period,
        period_param: period_param,
        summary: summary,
        template_items_count: template_items_count %>
<% end %>
